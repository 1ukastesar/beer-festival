{
  "name": "beer-festival",
  "version": "1.0.0",
  "description": "Pivní slavnosti – live hlasování piv (Vercel + Neon Postgres)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^5.8.26",
    "typescript": "^5.5.0"
  }
}
